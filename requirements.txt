# ========================================
# 小说生成器依赖 - 支持两种部署方式
# ========================================

# 核心依赖
torch>=2.0.0
transformers>=4.45.0
accelerate>=1.0.0
sentencepiece
protobuf>=3.20.0

# ----------------------------------------
# 方式1: Gradio 前端 + transformers 直接加载 AWQ 模型
# ----------------------------------------
gradio>=4.44.0

# AWQ 量化支持 (用于量化模型和直接加载)
autoawq>=0.2.0

# ----------------------------------------
# 方式2: Streamlit 前端 + vLLM 服务器
# ----------------------------------------
streamlit>=1.40.0
# vllm>=0.6.0  # 可选，仅在使用 vLLM 部署时需要

# ----------------------------------------
# 其他工具
# ----------------------------------------
datasets>=3.0.0
ninja
einops
hydra-core

# 开发依赖 (可选)
# peft>=0.13.0
# bitsandbytes>=0.43.0
