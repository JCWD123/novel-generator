# ========================================
# 小说生成器依赖 - Gradio + AWQ 部署
# ========================================

# 核心依赖 (版本锁定以确保兼容性)
torch>=2.0.0,<2.7.0
transformers==4.51.3  # 必须锁定! autoawq 最后测试版本
accelerate>=1.0.0
sentencepiece
protobuf>=3.20.0

# ----------------------------------------
# Gradio 前端
# ----------------------------------------
gradio>=4.44.0

# ----------------------------------------
# AWQ 量化支持
# ----------------------------------------
autoawq>=0.2.0  # 注意: 已弃用，但仍可用

# ----------------------------------------
# 可选依赖
# ----------------------------------------
# streamlit>=1.40.0  # 旧版 Streamlit 前端
# vllm>=0.6.0  # 生产环境推荐使用 vLLM 部署
# datasets>=3.0.0  # 仅量化时需要公开数据集
