# ========================================
# 环境变量配置示例
# ========================================
# 复制此文件为 .env 并修改配置
# cp env.example .env
# ========================================

# ==================== 模型配置 ====================
# 模型目录 (宿主机路径)
MODEL_DIR=/home/birdi/models

# 模型路径 (容器内路径，可自定义，只要与挂载对应)
MODEL_PATH=/models/DeepSeek-R1-1.5B-AWQ

# API 模型名称（前端/调用时用，可自定义）
MODEL_NAME=deepseek-r1-1.5b

# ==================== vLLM 配置 ====================
# 服务端口
VLLM_PORT=8000

# 张量并行 GPU 数量
TENSOR_PARALLEL_SIZE=1

# 显存利用率 (0.0-1.0)
GPU_MEMORY_UTILIZATION=0.9

# 最大序列长度
MAX_MODEL_LEN=4096

# 数据类型
DTYPE=float16

# 量化方式 (awq / 留空表示不量化)
QUANTIZATION=awq

# 是否强制 eager 模式
ENFORCE_EAGER=true

# ==================== Streamlit 配置 ====================
# 前端端口
STREAMLIT_PORT=8501

# ==================== GPU 配置 ====================
# 可见 GPU 设备
CUDA_VISIBLE_DEVICES=0
NVIDIA_VISIBLE_DEVICES=all

